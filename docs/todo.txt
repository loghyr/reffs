<!--
SPDX-FileCopyrightText: 2026 Tom Haynes <loghyr@gmail.com>
SPDX-License-Identifier: AGPL-3.0-or-later
-->

1) Impelement a RocksDB backend

2) Impelement IO to files via iouring
   a) Separate queues?

3) Make a config file
   a) json/yaml/libconfig ?
   b) What type of NFSv4.2 server?
   c) Tunings for iouring

4) Gather per-op NFSv4.2 stats
   a) We do protocol level for NULL and COMPOUND

5) Gather NFSv4.2 errors reported
   a) All
   b) By Super-Block
   c) By Client

6) The NFSv4.2 callback operations
   a) CB_GETATTR: must not stack many requests for same inode — deduplicate
   b) CB_GETATTR: pause GETATTR compound, issue on back-channel, decode reply
      timestamps; currently GETATTR falls back to server-side values for files
      with OPEN_DELEGATE_READ/WRITE_ATTRS_DELEG (RFC 9754 timestamp delegation)
   c) Are we pausing the compound/request or are we waiting by burning a thread?
   d) Need a compound state machine

7) RFC 9754 support
   a) open XOR delegation stateid  <-- done
   b) timestamp delegation (OPEN_DELEGATE_*_ATTRS_DELEG, ds_timestamps)  <-- done
   c) SETATTR TIME_DELEG_ACCESS / TIME_DELEG_MODIFY  <-- done
   d) CB_GETATTR for authoritative delegated timestamps  <-- depends on item 6

8) Why does the lifecyle never leave grace started?  <-- fixed (sps_clean_shutdown clobber + NLM vs NFS4 grace)

9) Full client recovery
